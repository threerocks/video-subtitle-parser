# Windsurf Cascade Rule

For supported video URLs, run `video-subtitle-parser` into a dedicated `materials/` directory before analysis.

Prefer subtitles when available. Use ASR fallback only when captions are unavailable, blocked, empty, or rate-limited.

Generated transcripts and metadata are the handoff files for downstream writing.

# Claude Code Command: parse-video

Parse the provided video URL with Video Subtitle Parser.

```bash
video-subtitle-parser "$ARGUMENTS" --platform auto --out-dir materials/video-source
```

Then inspect the generated metadata and cleaned transcript. Use the transcript as the source of truth unless the user explicitly asks for outside research.
